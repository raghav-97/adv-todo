import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";

export default function AddToList() {
  return (
    <Card>
      <CardContent>
        <div>
          {/* Add + sign here */}
          Add List
        </div>
      </CardContent>
    </Card>
  );
}
